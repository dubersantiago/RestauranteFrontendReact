# Variables de entorno necesarias en runtime para el frontend
# VITE_API_URL debe apuntar a la URL base del backend
# Ejemplo: /api cuando el backend está disponible en la misma máquina/proxy
VITE_API_URL=/api
